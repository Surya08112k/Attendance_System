server.port=${PORT:8080}
spring.application.name=AI Infotech Attendance System

# Excel file path — on Render/Railway, override with ATTENDANCE_EXCEL_PATH env var
# pointing to a persistent disk (e.g. /data/attendance_records.xlsx)
attendance.excel.path=${ATTENDANCE_EXCEL_PATH:attendance_records.xlsx}

# Allow CORS for frontend
spring.web.resources.static-locations=classpath:/static/

# ─── Email Configuration (Resend HTTPS API) ─────────────────────
resend.api.key=${RESEND_API_KEY:PASTE_YOUR_RESEND_API_KEY_HERE}
resend.from.email=${RESEND_FROM_EMAIL:onboarding@resend.dev}

# ─── Email Report Settings ───────────────────────────────────────
attendance.email.to=${ATTENDANCE_EMAIL_TO:sr9941101@gmail.com}
attendance.email.from-name=AI Infotech Attendance System
attendance.email.daily-cron=0 0 18 * * *
attendance.email.weekly-cron=0 0 8 * * MON

# ─── Admin Access ─────────────────────────────────────────────────
admin.access.id=${ADMIN_ACCESS_ID:ADMIN123}

# ─── Geo-Fencing Configuration ───────────────────────────────────
attendance.geo.office-lat=${GEO_LAT:13.0827}
attendance.geo.office-lon=${GEO_LON:80.2707}
attendance.geo.radius-meters=${GEO_RADIUS:1000}
attendance.geo.enforce=${GEO_ENFORCE:true}
